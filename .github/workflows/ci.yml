name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax check
        run: |
          for f in $(find . -maxdepth 1 -name '*.js' -o -maxdepth 1 -name '*.mjs' | sort); do
            node --check "$f"
          done
